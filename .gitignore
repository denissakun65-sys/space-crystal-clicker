# Зависимости и логи
node_modules/
npm-debug.log*
yarn-error.log*

# Переменные окружения и локальная конфигурация
.env
.env.local
*.local

# Системные файлы
.DS_Store
Thumbs.db

# Ключи не должны попадать в git-историю, если вы их когда-либо закоммитите
# (лучше не хранить apiKey в config.js публично — используйте ограничение
#  доменов в Firebase Console: Authentication -> Authorized domains)
